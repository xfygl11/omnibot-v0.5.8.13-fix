<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/permission_overlay_bg"
    android:clickable="true"
    android:focusable="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginHorizontal="24dp"
        android:background="@drawable/permission_card_bg"
        android:orientation="vertical"
        android:padding="20dp">

        <LinearLayout
            android:id="@+id/permission_purpose_box"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/permission_purpose_box_bg"
            android:orientation="vertical"
            android:padding="12dp">

            <TextView
                android:id="@+id/permission_label"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="@color/permission_primary_text"
                android:textSize="14sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/permission_purpose"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:textColor="@color/permission_secondary_text"
                android:textSize="14sp" />
        </LinearLayout>

        <!-- 分隔线 -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginTop="16dp"
            android:background="@color/permission_divider" />

        <!-- 是否允许 APP [权限操作] -->
        <TextView
            android:id="@+id/permission_prompt"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:lineSpacingMultiplier="1.2"
            android:textColor="@color/permission_primary_text"
            android:textSize="15sp" />

        <!-- 禁止 | 允许 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:layout_marginTop="20dp"
            android:gravity="center"
            android:orientation="horizontal">

            <Button
                android:id="@+id/btn_deny"
                style="@style/Widget.PermissionRequest.Button"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:text="@string/permission_btn_deny_text" />

            <View
                android:layout_width="1dp"
                android:layout_height="match_parent"
                android:background="@color/permission_divider" />

            <Button
                android:id="@+id/btn_allow"
                style="@style/Widget.PermissionRequest.Button"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:textColor="@color/permission_btn_allow"
                android:text="@string/permission_btn_allow_text" />
        </LinearLayout>
    </LinearLayout>
</FrameLayout>
